---
interface Props {
  text: string;
  semanticLevel?: "h1" | "h2" | "h3" | "h4" | "h5" | "h6";
}

const { text, semanticLevel = "h2" } = Astro.props;

const Tag = semanticLevel;
---

{
  /* prettier-ignore */
  text && <Tag>{text}</Tag>
}

<style>
  /* Add any default heading styles here if needed */
</style>
